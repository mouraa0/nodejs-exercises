<%- include('includes/head.ejs')%>
    <% if (users.length > 0) {%>
        <% for (let usuarios of users) { %>
            <h1><%= usuarios.title %></h1>
        <% } %>
    <% } else {%>
        <h1>No Users</h1>
    <% } %>
<%- include('includes/end.ejs')%>